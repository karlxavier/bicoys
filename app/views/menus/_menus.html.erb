<div class="ibox-content" id="menus" style="display: block; padding: 15px 0 0 0;">
    <div>
        <table class="table shoping-cart-table">
            <tbody>
            	<% menu_cat.menus.each do |menu| %>
                    <tr>
                        <td width="80">
                        	<% if menu.image.present? %>
                            	<div class="productImage menu-image" style="width: 80px; height: 80px; background-image: url('<%= image_path menu.image.url(:web) %>'); "></div>
                            <% else %>
                            	<%= image_tag 'default_menu.png', class: 'productImage', style: 'width: 80px; height: 80px;' %>
                            <% end %>
                        </td>
                        <td class="desc">
                            <h4 class="text-navy">
                                <%= menu.name %>
                            </h4>
                            <h5>
                                ₱ <%= number_with_precision(menu.price, :precision => 2, :delimiter => ',') %>
                            </h5>
                            <p class="small">
                                <%= menu.description %>
                            </p>
                        </td>
                        <td>
                            <%= form_for order_item, remote: true do |f| %>      
                                <div class="input-group">
                                  <%= f.hidden_field :quantity, value: 1, min: 1 %>
                                  <div class="input-group-btn">
                                    <%= f.hidden_field :menu_id, value: menu.id %>
                                    <%= f.submit "Add", class: "btn btn-outline btn-primary add-sk" %>            
                                  </div>
                                </div>
                            <% end %>   
                        </td>
                    </tr>
                <% end %>

            </tbody>
        </table>
    </div>
</div>