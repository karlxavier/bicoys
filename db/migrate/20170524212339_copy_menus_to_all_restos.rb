class CopyMenusToAllRestos < ActiveRecord::Migration[5.0]
  def change
 #  	resto7 = Restaurant.find(9)
 #  	resto7.menus.find_each do |menu|
	# 	Restaurant.each do |resto|
	# 		resto.menus.create!(
	# 			:name => menu.name,
	# 			:description => menu.description,
	# 			:price => menu.price,
	# 			:restaurant_id => resto.id,
	# 			:total_price => menu.total_price,
	# 			:menu_category_id => menu.menu_category_id,
	# 			:commission => menu.commission,
	# 			:image => menu.image
	# 		)
	# 	end
	# end
  end
end
